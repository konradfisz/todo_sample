class Todo {
  final String id;
  final String title;
  final String description;
  final bool done;

  Todo({
    required this.id,
    required this.title,
    required this.description,
    required this.done,
  });
}
